<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Uploader</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="stylesheet" href="/styles/file.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body>
    <header>
        <h1>File Uploader</h1>
        <div class="user-menu">
            <div class="user-avatar">
                <%= user.name.charAt(0).toUpperCase() %>
            </div>
            <div class="dropdown-content" role="menu">
                <a href="/auth/logout" role="menuitem">Logout</a>
            </div>
        </div>
    </header>

    <main class="main">
        <nav class="sidebar">
            <div class="sidebar-item" onclick="window.location.href='/'" role="button" tabindex="0">
                <i class="material-icons">home</i>
                <span>Home</span>
            </div>
            <div class="sidebar-item active">
                <i class="material-icons">folder</i>
                <span><%= file.name %></span>
            </div>
        </nav>

        <div class="content">
            <section class="file-details-container">
                <div class="file-preview">
                    <div class="file-icon-large">
                        <i class="material-icons">insert_drive_file</i>
                        <span class="file-extension"><%= file.type %></span>
                    </div>
                </div>

                <div class="file-info-details">
                    <h2><%= file.name %></h2>
                    
                    <dl class="file-properties">
                        <div class="property">
                            <dt class="property-label">Type:</dt>
                            <dd class="property-value"><%= file.type %></dd>
                        </div>
                        <div class="property">
                            <dt class="property-label">Size:</dt>
                            <dd class="property-value"><%= file.size %></dd>
                        </div>
                        <div class="property">
                            <dt class="property-label">Uploaded:</dt>
                            <dd class="property-value"><%= file.createdAt %></dd>
                        </div>
                        <div class="property">
                            <dt class="property-label">Location:</dt>
                            <dd class="property-value">
                                <% if (file.folderId) { %>
                                    Folder
                                <% } else { %>
                                    My Drive
                                <% } %>
                            </dd>
                        </div>
                    </dl>

                    <div class="file-actions">
                        <a href="<%= file.url %>" download="<%= file.name %>" class="download-button">
                            <i class="material-icons">download</i>
                            <span>Download</span>
                        </a>
                    </div>
                </div>
            </section>
        </div>
    </main>
</body>
</html>
